/// CLI 子模块根：聚合命令行上下文（`context`）、REPL 命令解析（`repl`）、无头模式（`headless`）。
/// CLI submodule root: aggregates the command-line context (`context`), REPL
/// command parsing (`repl`), and headless mode (`headless`).
pub mod context;
pub mod headless;
pub mod repl;
